# penalized.svd.R
# Run penalized.svd.R on samples

library(devtools)

dev_mode(on=T)

install_github("jakeyeung/PMA")

# use dev ggplot2 now

# when finished do:

dev_mode(on=F) #and you are back to having stable ggplot2
